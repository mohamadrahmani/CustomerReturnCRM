"use client";

import { useEffect, useState } from "react";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { createService, getServices, updateService, type Service } from "@/lib/api";
import { useAuth } from "@/components/auth-provider";

function money(value: number) { return `${value.toLocaleString("fa-IR")} تومان`; }
function LoadingRows() { return <div className="space-y-2">{Array.from({ length: 6 }).map((_, i) => <div key={i} className="h-20 animate-pulse rounded-2xl bg-white/70" />)}</div>; }
function Field({ label, required, children }: { label: string; required?: boolean; children: React.ReactNode }) { return <label className="block"><span className="mb-1.5 block text-xs font-bold text-slate-700">{label}{required && <span className="mr-1 text-pink-500">*</span>}</span>{children}</label>; }
function Status({ active }: { active: boolean }) { return active ? <span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-black text-emerald-700">فعال</span> : <span className="rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-black text-slate-500">غیرفعال</span>; }
function ReturnCycleStatus({ days }: { days: number | null }) { return days == null ? <span className="inline-flex items-center gap-1.5 rounded-full border border-rose-200 bg-gradient-to-l from-rose-50 to-red-50 px-2.5 py-1 text-[10px] font-black text-rose-700"><span className="h-1.5 w-1.5 rounded-full bg-rose-500" />تعریف نشده</span> : <span className="text-xs text-slate-500">{days.toLocaleString("fa-IR")} روز</span>; }

function ReturnCycleInfo() {
  return <span className="group relative inline-flex align-middle">
    <button type="button" aria-label="اطلاعات چرخه بازگشت" className="flex h-5 w-5 items-center justify-center rounded-full border border-slate-300 bg-white text-[11px] font-black text-slate-500 transition hover:border-pink-400 hover:text-pink-600">i</button>
    <span role="tooltip" className="pointer-events-none invisible absolute left-0 top-7 z-50 w-64 max-w-[calc(100vw-3rem)] rounded-xl border border-slate-200 bg-slate-900 px-3 py-2.5 text-right text-[11px] font-medium leading-5 text-white opacity-0 shadow-xl transition group-hover:visible group-hover:opacity-100">اگر دوره بازگشت را وارد نکنید، سیستم نمی‌تواند تاریخ بازگشت مورد انتظار را محاسبه کند و تحلیل بازگشت برای این خدمت انجام نمی‌شود.</span>
  </span>;
}

type Form = { title: string; description: string; defaultPrice: string; defaultDurationMinutes: string; suggestedReturnDays: string; isActive: boolean };
const emptyForm: Form = { title: "", description: "", defaultPrice: "", defaultDurationMinutes: "60", suggestedReturnDays: "", isActive: true };

export default function ServicesPage() {
  const { auth, businessId, isReady } = useAuth();
  const activeBusinessId = businessId ?? auth?.businesses[0]?.id ?? null;
  const queryClient = useQueryClient();
  const [searchInput, setSearchInput] = useState("");
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState<"active" | "inactive" | "all">("active");
  const [page, setPage] = useState(1);
  const [editing, setEditing] = useState<Service | null>(null);
  const [isCreateOpen, setCreateOpen] = useState(false);
  const [form, setForm] = useState<Form>(emptyForm);
  const [error, setError] = useState("");
  const [saving, setSaving] = useState(false);

  useEffect(() => { const timer = window.setTimeout(() => { setSearch(searchInput.trim()); setPage(1); }, 300); return () => window.clearTimeout(timer); }, [searchInput]);
  const isActive = status === "all" ? null : status === "active";
  const query = useQuery({ queryKey: ["services", activeBusinessId, page, search, status], queryFn: () => getServices(activeBusinessId!, page, 12, search, isActive), enabled: isReady && !!activeBusinessId, staleTime: 15_000 });

  if (!isReady || !activeBusinessId) return <main className="p-4"><LoadingRows /></main>;
  const data = query.data;
  const openCreate = () => { setForm(emptyForm); setError(""); setEditing(null); setCreateOpen(true); };
  const openEdit = (service: Service) => { setForm({ title: service.title, description: service.description ?? "", defaultPrice: String(service.defaultPrice), defaultDurationMinutes: String(service.defaultDurationMinutes), suggestedReturnDays: service.suggestedReturnDays == null ? "" : String(service.suggestedReturnDays), isActive: service.isActive }); setError(""); setEditing(service); setCreateOpen(true); };
  const close = () => { if (!saving) setCreateOpen(false); };
  async function submit(event: React.FormEvent) {
    event.preventDefault(); setError("");
    const price = Number(form.defaultPrice); const duration = Number(form.defaultDurationMinutes); const returnDays = form.suggestedReturnDays.trim() === "" ? undefined : Number(form.suggestedReturnDays);
    if (!form.title.trim()) return setError("عنوان خدمت الزامی است.");
    if (!Number.isFinite(price) || price < 0) return setError("قیمت باید عددی صفر یا بیشتر باشد.");
    if (!Number.isInteger(duration) || duration <= 0) return setError("مدت خدمت باید عدد صحیح بزرگ‌تر از صفر باشد.");
    if (returnDays !== undefined && (!Number.isInteger(returnDays) || returnDays < 0)) return setError("چرخه بازگشت باید عدد صحیح صفر یا بیشتر باشد.");
    setSaving(true);
    try {
      if (editing) await updateService(activeBusinessId!, editing.id, { title: form.title.trim(), description: form.description.trim() || undefined, defaultPrice: price, defaultDurationMinutes: duration, suggestedReturnDays: returnDays, isActive: form.isActive });
      else await createService(activeBusinessId!, { title: form.title.trim(), description: form.description.trim() || undefined, defaultPrice: price, defaultDurationMinutes: duration, suggestedReturnDays: returnDays });
      setCreateOpen(false); await queryClient.invalidateQueries({ queryKey: ["services", activeBusinessId] });
    } catch (e) { setError(e instanceof Error ? e.message : "ذخیره خدمت انجام نشد."); } finally { setSaving(false); }
  }

  return <main className="-mx-4 -mt-5 min-h-[calc(100vh-65px)] bg-gradient-to-b from-rose-50 via-pink-50/60 to-violet-50/40 px-4 pb-10 pt-5 sm:-mx-8 sm:-mt-8 sm:px-8 sm:pt-7">
    <div className="mx-auto max-w-7xl">
      <header className="mb-5 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[11px] font-bold text-pink-600">تنظیمات عملیاتی</p><h1 className="mt-1 text-2xl font-black tracking-tight text-slate-950 sm:text-3xl">خدمات</h1><p className="mt-1 text-xs leading-5 text-slate-500 sm:text-sm">خدمات قابل ارائه، قیمت و چرخه پیشنهادی بازگشت را مدیریت کنید.</p></div><button onClick={openCreate} className="crm-action self-start !min-h-10 !rounded-xl !px-4 !py-2.5 !text-xs bg-gradient-to-l from-pink-500 to-rose-500 shadow-md shadow-pink-200 sm:self-auto sm:!text-sm">＋ خدمت جدید</button></header>
      <section className="crm-card mb-4 p-3.5 sm:p-4"><div className="flex flex-col gap-3 lg:flex-row lg:items-center"><label className="relative min-w-0 flex-1"><span className="sr-only">جستجوی خدمت</span><svg className="pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8"><circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/></svg><input value={searchInput} onChange={e => setSearchInput(e.target.value)} placeholder="جستجو در عنوان یا توضیحات خدمت..." className="crm-input w-full !pr-10" /></label><div className="grid grid-cols-3 gap-1 rounded-xl bg-slate-100 p-1 lg:w-[300px]">{([['active','فعال'],['inactive','غیرفعال'],['all','همه']] as const).map(([v,l]) => <button key={v} onClick={() => { setStatus(v); setPage(1); }} aria-pressed={status === v} className={`rounded-lg px-3 py-2 text-[11px] font-bold ${status === v ? "bg-white text-pink-600 shadow-sm ring-1 ring-slate-200/70" : "text-slate-500"}`}>{l}</button>)}</div></div></section>
      {query.isLoading ? <LoadingRows /> : query.isError ? <section className="crm-card p-8 text-center"><h2 className="font-black">بارگذاری خدمات انجام نشد</h2><button onClick={() => query.refetch()} className="crm-secondary-action mt-4">تلاش مجدد</button></section> : !data?.items.length ? <section className="crm-card p-10 text-center"><h2 className="text-lg font-black text-slate-900">خدمتی پیدا نشد</h2><p className="mt-1 text-sm text-slate-500">فیلترها را تغییر دهید یا خدمت جدیدی ثبت کنید.</p></section> : <>
        <section className="hidden overflow-hidden rounded-3xl border border-white/80 bg-white/95 shadow-sm md:block"><div className="grid grid-cols-[minmax(240px,1.5fr)_150px_120px_150px_90px_90px] items-center gap-4 border-b border-slate-100 px-6 py-3 text-[11px] font-black text-slate-400"><span>خدمت</span><span>قیمت</span><span>مدت</span><span>چرخه بازگشت</span><span>وضعیت</span><span></span></div>{data.items.map(s => <button key={s.id} onClick={() => openEdit(s)} className="grid w-full grid-cols-[minmax(240px,1.5fr)_150px_120px_150px_90px_90px] items-center gap-4 border-b border-slate-100 px-6 py-3.5 text-right transition last:border-0 hover:bg-pink-50/40"><div className="min-w-0"><p className="truncate text-sm font-black text-slate-900">{s.title}</p><p className="mt-0.5 truncate text-[11px] text-slate-400">{s.description || "بدون توضیحات"}</p></div><span className="text-xs font-bold text-slate-700">{money(s.defaultPrice)}</span><span className="text-xs text-slate-500">{s.defaultDurationMinutes.toLocaleString("fa-IR")} دقیقه</span><ReturnCycleStatus days={s.suggestedReturnDays} /><Status active={s.isActive} /><span className="text-xs font-black text-pink-500">ویرایش ←</span></button>)}</section>
        <section className="space-y-2 md:hidden">{data.items.map(s => <button key={s.id} onClick={() => openEdit(s)} className="block w-full rounded-2xl border border-white/80 bg-white/95 p-4 text-right shadow-sm"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate text-sm font-black text-slate-900">{s.title}</p><p className="mt-1 text-[11px] text-slate-400">{s.description || "بدون توضیحات"}</p></div><Status active={s.isActive} /></div><div className="mt-3 grid grid-cols-3 gap-2 border-t border-slate-100 pt-2.5 text-[10px]"><div><span className="text-slate-400">قیمت</span><p className="mt-1 font-bold text-slate-700">{money(s.defaultPrice)}</p></div><div><span className="text-slate-400">مدت</span><p className="mt-1 font-bold text-slate-700">{s.defaultDurationMinutes.toLocaleString("fa-IR")} دقیقه</p></div><div><span className="text-slate-400">بازگشت</span><p className={`mt-1 font-bold ${s.suggestedReturnDays == null ? "text-rose-600" : "text-slate-700"}`}>{s.suggestedReturnDays == null ? "تعریف نشده" : `${s.suggestedReturnDays.toLocaleString("fa-IR")} روز`}</p></div></div></button>)}</section>
        <div className="mt-4 flex items-center justify-between rounded-2xl border border-white/80 bg-white/80 px-3 py-2.5 text-[11px] font-bold text-slate-500"><span>{data.totalCount.toLocaleString("fa-IR")} خدمت</span><div className="flex items-center gap-1.5"><button disabled={data.page <= 1} onClick={() => setPage(p => p - 1)} className="rounded-lg border border-slate-200 bg-white px-3 py-1.5 disabled:opacity-40">قبلی</button><span className="px-1">{data.page.toLocaleString("fa-IR")} / {data.totalPages.toLocaleString("fa-IR")}</span><button disabled={data.page >= data.totalPages} onClick={() => setPage(p => p + 1)} className="rounded-lg border border-slate-200 bg-white px-3 py-1.5 disabled:opacity-40">بعدی</button></div></div>
      </>}
    </div>
    {isCreateOpen && <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/35 p-0 backdrop-blur-[3px] sm:items-center sm:p-4" onMouseDown={close}><div role="dialog" aria-modal="true" className="max-h-[92vh] w-full max-w-lg overflow-y-auto rounded-t-[28px] bg-white shadow-2xl sm:rounded-[28px]" onMouseDown={e => e.stopPropagation()}><div className="border-b border-slate-100 bg-gradient-to-l from-pink-50 to-white px-5 py-4"><div className="flex items-start justify-between"><div><p className="text-[11px] font-bold text-pink-600">{editing ? "ویرایش خدمت" : "خدمت جدید"}</p><h2 className="mt-1 text-xl font-black">{editing ? editing.title : "ثبت خدمت"}</h2></div><button onClick={close} className="h-10 w-10 rounded-xl border border-slate-200 text-lg text-slate-500">×</button></div></div><form onSubmit={submit} className="space-y-4 px-5 py-5 sm:px-6"><div className="grid gap-4 sm:grid-cols-2"><Field label="عنوان خدمت" required><input autoFocus value={form.title} onChange={e => setForm({...form,title:e.target.value})} className="crm-input w-full" placeholder="مثلاً اصلاح مو" /></Field><Field label="قیمت پیش‌فرض" required><input inputMode="decimal" value={form.defaultPrice} onChange={e => setForm({...form,defaultPrice:e.target.value})} className="crm-input w-full" placeholder="مثلاً 500000" /></Field></div><Field label="توضیحات"><textarea value={form.description} onChange={e => setForm({...form,description:e.target.value})} className="crm-input min-h-24 w-full resize-y" placeholder="توضیح کوتاه درباره خدمت..." /></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="مدت (دقیقه)" required><input inputMode="numeric" value={form.defaultDurationMinutes} onChange={e => setForm({...form,defaultDurationMinutes:e.target.value})} className="crm-input w-full" /></Field><Field label={<span className="inline-flex items-center gap-1.5">چرخه پیشنهادی بازگشت (روز)<ReturnCycleInfo /></span>}><div className="space-y-1.5"><input inputMode="numeric" value={form.suggestedReturnDays} onChange={e => setForm({...form,suggestedReturnDays:e.target.value})} className={`crm-input w-full transition ${form.suggestedReturnDays.trim() === "" ? "border-rose-400 bg-gradient-to-l from-rose-50/70 to-white shadow-[0_0_0_3px_rgba(244,63,94,0.08)] placeholder:text-rose-300 focus:border-rose-500 focus:ring-rose-200" : "border-emerald-300 bg-emerald-50/20"}`} placeholder="مثلاً ۳۰" />{form.suggestedReturnDays.trim() === "" && <p className="text-[10px] font-bold leading-4 text-rose-600">برای انجام تحلیل بازگشت این خدمت، دوره بازگشت را وارد کنید.</p>}</div></Field></div>{editing && <label className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-3 text-xs font-bold"><span>وضعیت خدمت</span><button type="button" onClick={() => setForm({...form,isActive:!form.isActive})} className={`rounded-full px-3 py-1.5 ${form.isActive ? "bg-emerald-100 text-emerald-700" : "bg-slate-200 text-slate-500"}`}>{form.isActive ? "فعال" : "غیرفعال"}</button></label>}{error && <p className="rounded-xl bg-rose-50 px-3 py-2 text-xs font-bold leading-5 text-rose-700">{error}</p>}<div className="flex gap-2 border-t border-slate-100 pt-4"><button disabled={saving} className="crm-action flex-1 !min-h-11 !rounded-xl">{saving ? "در حال ذخیره..." : "ذخیره"}</button><button type="button" disabled={saving} onClick={close} className="crm-secondary-action !min-h-11 !rounded-xl">انصراف</button></div></form></div></div>}
  </main>;
}
